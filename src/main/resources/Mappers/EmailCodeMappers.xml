<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boollan.dao.IEmailCodeDao">

    <!--配置实体类数据-->
    <resultMap id="BaseUserResult" type="com.boollan.domain.emailcode">
        <id column="ID" property="id" jdbcType="VARCHAR"/>
        <result column="EMAIL" property="email" jdbcType="VARCHAR"/>
        <result column="CODE" property="code" jdbcType="VARCHAR"/>
        <result column="EFFECTIVE" property="effective" jdbcType="VARCHAR"/>
        <result column="ISUSE" property="isuse" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insetCreatEmailCode" parameterType="com.boollan.domain.emailcode">
        insert into emailcode(email,isuse,code,effective)values(#{email},#{isuse},#{code},#{effective})
    </insert>
    <update id="updateEmailCode" parameterType="com.boollan.domain.emailcode">
        update emailcode set isuse=#{isuse},effective=#{effective} where email=#{email} and code=#{code}
    </update>
    <select id="findAllEmailCode">
        select * from emailcode
    </select>
    <select id="findCodeByEamil">
        select * from emailcode where email=#{email} and code=#{code}
    </select>

</mapper>